#!/usr/bin/env bash
set -euo pipefail

if command -v mise >/dev/null 2>&1; then
  exec mise exec java@17 -- gradle "$@"
fi

if [[ "${JAVA_HOME:-}" != "" ]]; then
  PATH="$JAVA_HOME/bin:$PATH" exec gradle "$@"
fi

echo "JAVA_HOME is not set. Configure a JDK 17 install and re-run." >&2
exit 1
